const fs=require('fs'); const path='react/resources/js/Pages/admin-it-chats.tsx'; let text=fs.readFileSync(path,'utf8'); text=text.replace('<CardTitle className= text-base>Tiket Saya</CardTitle>', '<CardTitle className=text-base>Tiket Aktif</CardTitle>'); text=text.replace('<CardDescription>Daftar tiket yang bisa Anda chat-kan langsung</CardDescription>', '<CardDescription className=text-sm text-muted-foreground>\r\n                  Daftar tiket yang dapat Anda tangani dan balas\r\n                </CardDescription>'); const newContent=<CardContent className=space-y-3>\r\n                {loadingTickets ? (\r\n                  <p className=text-sm text-muted-foreground>Memuat tiket...</p>\r\n                ) : activeTickets.length === 0 ? (\r\n                  <p className=text-sm text-muted-foreground>Tidak ada tiket aktif</p>\r\n                ) : (\r\n                  activeTickets.map(renderTicketItem)\r\n                )}\r\n              </CardContent>; text=text.replace(/<CardContent className=space-y-3>[\s\S]*?<\/CardContent>/, newContent); fs.writeFileSync(path,text,'utf8');
